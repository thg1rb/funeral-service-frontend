services:
    funeral-service-frontend:
        build: .
        ports:
            - "3000:3000"
        volumes:
            - .:/app
            - bun_modules:/app/node_modules
        environment:
            NODE_ENV: development
        restart: unless-stopped

volumes:
    bun_modules:
